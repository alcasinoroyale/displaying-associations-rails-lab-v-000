<h1><%= @artist.name %></h1>

<h1><%= @artist.name %></h1>

<h3><%= pluralize(@artist.songs.count, 'Song') %></h3>
<ul>
  <% @artist.songs.each do |s| %>
    <li><%= link_to s.title, post_path(p) %></li>
  <% end %>
</ul>
