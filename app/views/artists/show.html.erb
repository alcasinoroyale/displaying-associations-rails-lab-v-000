<h1><%= @artist.name %></h1>

<h1><%= @artist.name %></h1>

<h3><%= pluralize(@artist.songs.count, 'Song') %></h3>
<ul>
  <% @artist.posts.each do |p| %>
    <li><%= link_to p.title, post_path(p) %></li>
  <% end %>
</ul>
